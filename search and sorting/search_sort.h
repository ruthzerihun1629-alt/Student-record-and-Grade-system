#ifndef SEARCH_SORT_H
#define SEARCH_SORT_H

#include "student.h"

Student searchByName(vector<Student>& students, string name);

void sortByMarks(vector<Student>& students);

void sortByName(vector<Student>& students);

#endif